{
  "name": "spbb-cc",
  "version": "1.0.0",
  "private": true,
  "description": "Scotts Park Bike Bus — static landing page hosted on Cloudflare Pages",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name spbb-cc"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
